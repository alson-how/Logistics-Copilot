server {
    listen 80;
    listen [::]:80;
    server_name chatlah.tech;

    # Handle ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/html;
        try_files $uri =404;
    }

    # Serve the application over HTTP (temporary until SSL is set up)
    location / {
        proxy_pass http://web:5173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /api/ {
        proxy_pass http://api:8080/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS server - commented out until we have certificates
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name chatlah.tech;
#
#     ssl_certificate /etc/letsencrypt/live/chatlah.tech/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/chatlah.tech/privkey.pem;
#
#     location / {
#         proxy_pass http://web:5173;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade ;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host ;
#         proxy_cache_bypass ;
#     }
#
#     location /api/ {
#         proxy_pass http://api:8080/;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade ;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host ;
#         proxy_cache_bypass ;
#     }
# }